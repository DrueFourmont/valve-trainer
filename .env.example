# Copy to .env.local and fill in from your Supabase project:
# Project Settings, then API. Use the Project URL and the anon public key.
# .env.local is gitignored and must never be committed.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
