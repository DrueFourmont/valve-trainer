# Models

Drop `skid.glb` here.

Until it exists the app falls back to the primitive placeholder in
`src/scene/skid.ts`. See CLAUDE.md and the Blender export notes in the README
for the node names and origins the app expects.
